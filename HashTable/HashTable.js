class HashTable {
    constructor() {
        this.hashtable = [];
    }
    hash(k, m) {
            return int(k % 9);
        } //m is size of hash table
    add(key, value) {

        } //if key already exists, update value
    exists(key) {

    }
    get(key) {

    }
    remove(key) {

    }
}